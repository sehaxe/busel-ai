name: bench-shpak
stages:
  - name: pretrain
    data_preset: shpak
    checkpoint_out: /tmp/bench_shpak.pt
    params:
      profile_name: shpak
      max_steps: 15
      warmup_steps: 5
      use_schedule_free: true
global_params: {}
